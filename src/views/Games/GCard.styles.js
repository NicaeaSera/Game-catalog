const styles = theme => ({
  card: {
    display: 'flex',
  },
  cardDetails: {
    flex: 1,
  },
  cardMedia: {
    maxWidth: 500,
    width: '100%',
    maxHeight: 800,
  },
  cardDescription: {
    backgroundColor: theme.palette.background.paper,
  },
  main: {
    backgroundColor: theme.palette.background.paper,
  }
});

export default styles